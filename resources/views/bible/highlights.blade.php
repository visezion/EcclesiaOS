<x-app-layout title="Highlights" :breadcrumbs="$breadcrumbs" main-class="px-4 py-5 sm:px-6 lg:px-7">
    @php($palette = ['yellow' => ['Yellow', 'bg-amber-400', 'border-amber-400', 'bg-amber-50', 'text-amber-700'], 'green' => ['Green', 'bg-emerald-500', 'border-emerald-500', 'bg-emerald-50', 'text-emerald-700'], 'purple' => ['Purple', 'bg-violet-500', 'border-violet-500', 'bg-violet-50', 'text-violet-700'], 'pink' => ['Pink', 'bg-pink-400', 'border-pink-400', 'bg-pink-50', 'text-pink-700'], 'blue' => ['Blue', 'bg-blue-500', 'border-blue-500', 'bg-blue-50', 'text-blue-700']])
    <div x-data="{ open: {{ $errors->any() ? 'true' : 'false' }} }" class="space-y-5">
        @include('bible._tabs')
        <div class="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div><h1 class="flex items-center gap-3 text-2xl font-black text-slate-950"><i data-lucide="highlighter" class="size-6 text-violet-600"></i>Highlights</h1><p class="mt-1 text-sm text-slate-500">Your saved verses remain visibly highlighted by color.</p></div><div class="flex flex-wrap gap-2"><button type="button" @click="open = true" class="inline-flex items-center gap-2 rounded-lg bg-violet-600 px-4 py-2.5 text-sm font-bold text-white"><i data-lucide="highlighter" class="size-4"></i>Highlight New Verse</button><a href="{{ route('bible.highlights.export') }}" class="inline-flex items-center gap-2 rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-bold text-slate-700"><i data-lucide="download" class="size-4 text-violet-600"></i>Export</a></div></div>
        @if(session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-sm font-semibold text-emerald-700">{{ session('status') }}</div>@endif
        <section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="grid gap-3 sm:grid-cols-2 xl:grid-cols-6"><div class="rounded-xl bg-violet-50 p-4"><p class="text-xs font-bold text-violet-700">Total highlighted</p><p class="mt-2 text-3xl font-black text-slate-950">{{ $highlights->total() }}</p><p class="text-xs text-slate-500">saved verses</p></div>@foreach($palette as $key => [$name, $dotClass, $borderClass, $bgClass, $textClass])<a href="{{ route('bible.highlights', ['color' => $key]) }}" class="rounded-xl border-t-4 {{ $borderClass }} {{ $bgClass }} p-4 hover:shadow-md"><div class="flex items-center gap-2 text-sm font-bold {{ $textClass }}"><span class="size-3 rounded-full {{ $dotClass }}"></span>{{ $name }}</div><p class="mt-2 text-2xl font-black text-slate-950">{{ $colorCounts->get($key, 0) }}</p><p class="text-xs text-slate-500">{{ $highlights->total() ? number_format(($colorCounts->get($key, 0) / $highlights->total()) * 100, 1) : '0.0' }}%</p></a>@endforeach</div></section>
        <section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><form method="GET" action="{{ route('bible.highlights') }}" class="grid gap-2 border-b border-slate-100 p-4 lg:grid-cols-[minmax(0,1fr)_160px_170px_160px_auto]"><label class="relative"><i data-lucide="search" class="absolute left-3 top-3 size-4 text-slate-400"></i><input name="q" value="{{ $filters['q'] }}" class="h-10 w-full rounded-lg border border-slate-200 pl-9 pr-3 text-sm" placeholder="Search highlights..."></label><select name="color" class="h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">All Colors</option>@foreach($palette as $key => [$name])<option value="{{ $key }}" @selected($filters['color'] === $key)>{{ $name }}</option>@endforeach</select><select name="book" class="h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">All Books</option>@foreach($books as $book)<option value="{{ $book }}" @selected($filters['book'] === $book)>{{ $book }}</option>@endforeach</select><select name="tag" class="h-10 rounded-lg border border-slate-200 px-3 text-sm"><option value="">All Tags</option>@foreach($tags as $tag)<option value="{{ $tag }}" @selected($filters['tag'] === $tag)>{{ $tag }}</option>@endforeach</select><a href="{{ route('bible.highlights') }}" class="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-slate-200 px-3 text-sm font-bold text-slate-600"><i data-lucide="rotate-ccw" class="size-4"></i>Clear</a></form><div class="hidden grid-cols-[190px_1fr_150px_160px_170px_90px] gap-3 border-b border-slate-100 px-5 py-3 text-xs font-black text-slate-700 lg:grid"><span>Verse Reference</span><span>Highlighted Verse</span><span>Color</span><span>Meaning / Tag</span><span>Date</span><span>Actions</span></div><div class="divide-y divide-slate-100">@forelse($highlights as $highlight)@php([$name, $dotClass, $borderClass, $bgClass, $textClass] = $palette[$highlight->color] ?? $palette['yellow'])<article class="grid gap-3 border-l-4 {{ $borderClass }} p-4 transition hover:bg-slate-50 lg:grid-cols-[190px_1fr_150px_160px_170px_90px] lg:items-center"><div><h2 class="font-black text-violet-700">{{ $highlight->reference }}</h2><p class="text-xs text-slate-500">{{ $highlight->translation?->abbreviation }}</p></div><p class="text-sm leading-6 text-slate-700"><mark class="rounded px-1.5 py-1 font-semibold {{ $bgClass }} {{ $textClass }}">{{ Str::limit($highlight->snippet, 180) }}</mark></p><span class="flex items-center gap-2 text-sm font-semibold text-slate-700"><span class="size-3 rounded-full {{ $dotClass }}"></span>{{ $name }}</span><span class="inline-flex w-fit rounded-lg {{ $bgClass }} px-3 py-2 text-xs font-bold {{ $textClass }}">{{ $highlight->meaning ?: ($highlight->tags[0] ?? 'Reflection') }}</span><time class="text-xs text-slate-500">{{ $highlight->created_at->format('M d, Y') }}<span class="block">{{ $highlight->created_at->format('g:i A') }}</span></time><div class="flex gap-3 text-slate-500"><a href="{{ route('bible.index', ['translation' => $highlight->translation?->abbreviation, 'book' => trim((string) preg_replace('/\s+.*/', '', $highlight->reference)), 'chapter' => (int) preg_replace('/^[^0-9]*/', '', $highlight->reference)]) }}" title="Open in reader"><i data-lucide="book-open" class="size-4"></i></a><button type="button" title="Copy verse" onclick="navigator.clipboard?.writeText(@js($highlight->snippet))"><i data-lucide="copy" class="size-4"></i></button></div></article>@empty<div class="p-14 text-center text-sm text-slate-500">No highlights found. Highlight a verse to see it here.</div>@endforelse</div><div class="flex items-center justify-between p-4 text-xs text-slate-500"><span>Showing {{ $highlights->firstItem() ?: 0 }} to {{ $highlights->lastItem() ?: 0 }} of {{ $highlights->total() }} highlights</span>{{ $highlights->links() }}</div></section>
        <div x-cloak x-show="open" class="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><form method="POST" action="{{ route('bible.highlights.store') }}" class="w-full max-w-xl rounded-2xl bg-white p-6 shadow-2xl">@csrf<h2 class="text-xl font-black">Highlight New Verse</h2><div class="mt-5 grid gap-4 sm:grid-cols-2"><label class="text-sm font-bold">Translation<select name="translation_id" required class="mt-1.5 h-11 w-full rounded-lg border px-3">@foreach($translations as $translation)<option value="{{ $translation->id }}">{{ $translation->abbreviation }} — {{ $translation->name }}</option>@endforeach</select></label><label class="text-sm font-bold">Reference<input name="reference" required placeholder="Philippians 4:13" class="mt-1.5 h-11 w-full rounded-lg border px-3"></label><label class="text-sm font-bold sm:col-span-2">Verse text<textarea name="snippet" required rows="4" class="mt-1.5 w-full rounded-lg border p-3" placeholder="Enter the verse text..."></textarea></label><label class="text-sm font-bold">Color<select name="color" class="mt-1.5 h-11 w-full rounded-lg border px-3">@foreach($palette as $key => [$name])<option value="{{ $key }}">{{ $name }}</option>@endforeach</select></label><label class="text-sm font-bold">Meaning / Tag<input name="meaning" class="mt-1.5 h-11 w-full rounded-lg border px-3"></label></div><div class="mt-5 flex justify-end gap-2"><button type="button" @click="open = false" class="rounded-lg border px-4 py-2.5 text-sm font-bold">Cancel</button><button class="rounded-lg bg-violet-600 px-4 py-2.5 text-sm font-bold text-white">Save Highlight</button></div></form></div>
    </div>
</x-app-layout>
