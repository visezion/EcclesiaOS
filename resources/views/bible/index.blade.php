<x-app-layout title="Bible" :breadcrumbs="$breadcrumbs" main-class="px-4 py-5 sm:px-6 lg:px-7">
    <div x-data="{ selectedVerses: [], actionOpen: null, draftText: '', toggleVerse(verse) { const found = this.selectedVerses.findIndex(item => item.verse === verse.verse); this.selectedVerses = found === -1 ? [...this.selectedVerses, verse] : this.selectedVerses.filter(item => item.verse !== verse.verse); }, isSelected(verse) { return this.selectedVerses.some(item => item.verse === verse.verse); }, openAction(action) { this.draftText = this.selectedText; this.actionOpen = action; }, get selectedText() { return this.selectedVerses.map(item => item.text).join(' '); }, get selectedReference() { if (!this.selectedVerses.length) return ''; const first = this.selectedVerses[0]; const last = this.selectedVerses[this.selectedVerses.length - 1]; return first.reference === last.reference ? first.reference : `${first.book} ${first.chapter}:${first.verse}-${last.verse}`; } }" class="space-y-4" style="font-size: {{ data_get($settings, 'font_size', 16) }}px; line-height: {{ data_get($settings, 'line_spacing', 'comfortable') === 'compact' ? '1.5' : (data_get($settings, 'line_spacing') === 'spacious' ? '2.25' : '1.75') }};">
        <section class="flex flex-col gap-4 rounded-xl border border-slate-200 bg-white px-5 py-4 shadow-sm lg:flex-row lg:items-center lg:justify-between"><div class="flex items-center gap-3"><span class="grid size-11 place-items-center rounded-xl bg-violet-100 text-violet-700"><i data-lucide="book-open" class="size-6"></i></span><div><h1 class="flex items-center gap-2 text-2xl font-black text-slate-950">Bible <i data-lucide="bookmark" class="size-5"></i></h1><p class="text-sm text-slate-500">Explore, study and grow in Godâ€™s Word.</p></div></div><div class="flex items-center gap-3"><a href="{{ route('bible.plans') }}" class="inline-flex items-center gap-2 rounded-lg bg-violet-600 px-5 py-3 text-sm font-bold text-white"><i data-lucide="calendar-days" class="size-4"></i>Reading Plans</a></div></section>
        <nav class="flex gap-7 overflow-x-auto border-b border-slate-200 px-2 text-sm font-bold text-slate-500"><a href="{{ route('bible.index') }}" class="border-b-2 border-violet-600 px-1 py-3 text-violet-700">Read</a><a href="{{ route('bible.plans') }}" class="px-1 py-3">Plans</a><a href="{{ route('bible.bookmarks') }}" class="px-1 py-3">Bookmarks</a><a href="{{ route('bible.notes') }}" class="px-1 py-3">Notes</a><a href="{{ route('bible.highlights') }}" class="px-1 py-3">Highlights</a></nav>
        <div class="grid gap-3 xl:grid-cols-[minmax(0,1fr)_250px_300px]">
            <section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><form method="GET" action="{{ route('bible.index') }}" class="grid gap-3 border-b border-slate-100 p-3 sm:grid-cols-[minmax(0,1fr)_minmax(0,1fr)_minmax(0,1fr)_auto]"><label class="text-[11px] font-bold uppercase text-slate-500">Version<select name="translation" class="mt-1 h-11 w-full rounded-lg border border-slate-200 px-3 text-sm">@foreach($translations as $item)<option value="{{ $item->abbreviation }}" @selected($translation?->id === $item->id)>{{ $item->abbreviation }} â€” {{ $item->name }}</option>@endforeach</select></label><label class="text-[11px] font-bold uppercase text-slate-500">Book<input name="book" value="{{ $book }}" list="bible-books" autocomplete="off" class="mt-1 h-11 w-full rounded-lg border border-slate-200 px-3 text-sm" placeholder="Search all books..."><datalist id="bible-books">@foreach($books as $item)<option value="{{ $item }}">{{ $item }}</option>@endforeach</datalist></label><label class="text-[11px] font-bold uppercase text-slate-500">Chapter<select name="chapter" class="mt-1 h-11 w-full rounded-lg border border-slate-200 px-3 text-sm">@forelse($chapters as $number)<option value="{{ $number }}" @selected($chapter === (int) $number)>Chapter {{ $number }}</option>@empty<option value="1">No chapters available</option>@endforelse</select><small class="mt-1 block normal-case font-normal text-slate-400">{{ $chapters->count() }} chapters in {{ $book }}</small></label><button class="grid size-11 place-items-center self-end rounded-lg bg-violet-600 text-white" title="Open"><i data-lucide="search" class="size-5"></i></button></form>
                <article class="p-5 sm:p-6"><div class="flex items-start justify-between"><div><p class="text-sm font-semibold text-slate-500">{{ $translation?->name }}</p><h2 class="mt-1 text-xl font-black text-slate-950">{{ $book }} {{ $chapter }}</h2><p class="mt-1 text-sm text-slate-500">{{ $translation?->name }} Â· Chapter {{ $chapter }}</p></div><a href="{{ route('bible.settings') }}" class="grid size-10 place-items-center rounded-lg border border-slate-200"><i data-lucide="settings-2" class="size-4"></i></a></div><div x-cloak x-show="selectedVerses.length" class="sticky top-2 z-10 mt-5 flex flex-wrap items-center gap-2 rounded-xl border border-violet-200 bg-violet-50 p-3 shadow-sm"><span class="mr-auto text-xs font-bold text-violet-800"><span x-text="selectedVerses.length"></span> verse<span x-show="selectedVerses.length !== 1">s</span> selected</span><button type="button" @click="openAction('bookmark')" class="inline-flex items-center gap-1 rounded-lg bg-violet-600 px-3 py-2 text-xs font-bold text-white"><i data-lucide="bookmark-plus" class="size-3.5"></i>Bookmark</button><button type="button" @click="openAction('note')" class="inline-flex items-center gap-1 rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold text-white"><i data-lucide="notebook-pen" class="size-3.5"></i>Add Note</button><button type="button" @click="openAction('highlight')" class="inline-flex items-center gap-1 rounded-lg bg-orange-500 px-3 py-2 text-xs font-bold text-white"><i data-lucide="highlighter" class="size-3.5"></i>Highlight</button><button type="button" @click="selectedVerses = []" class="rounded-lg border border-violet-200 px-3 py-2 text-xs font-bold text-violet-700">Clear</button></div><div class="mt-7 space-y-2 text-[15px] leading-7 text-slate-800">@forelse($verses as $verse)@php($highlightColor = $highlightColors[$verse->verse] ?? null)@php($highlightClass = match ($highlightColor) { 'green' => 'bg-emerald-100 text-emerald-950', 'purple' => 'bg-violet-100 text-violet-950', 'pink' => 'bg-pink-100 text-pink-950', 'blue' => 'bg-blue-100 text-blue-950', default => $highlightColor ? 'bg-yellow-100 text-yellow-950' : '' })@php($payload = ['translation_id' => $translation?->id, 'translation' => $translation?->abbreviation, 'reference' => $verse->book.' '.$verse->chapter.':'.$verse->verse, 'book' => $verse->book, 'chapter' => $verse->chapter, 'verse' => $verse->verse, 'text' => $verse->text])<button type="button" @click="toggleVerse({{ Js::from($payload) }})" class="group flex w-full items-start gap-3 rounded-xl border p-3 text-left transition" :class="isSelected({{ Js::from($payload) }}) ? 'border-violet-400 bg-violet-50 ring-1 ring-violet-200' : 'border-transparent hover:border-violet-100 hover:bg-violet-50/40'"><span class="mt-1 grid size-5 shrink-0 place-items-center rounded border" :class="isSelected({{ Js::from($payload) }}) ? 'border-violet-600 bg-violet-600 text-white' : 'border-slate-300 bg-white'"><i x-show="isSelected({{ Js::from($payload) }})" data-lucide="check" class="size-3.5"></i></span><sup class="mt-1 w-5 shrink-0 text-xs font-black text-slate-400">{{ $verse->verse }}</sup><span class="min-w-0 flex-1 rounded-md px-1.5 {{ $highlightClass }}">{{ $verse->text }}</span></button>@empty<p class="rounded-xl border border-dashed border-slate-300 bg-slate-50 p-6 text-sm text-slate-500">This chapter is not available in the selected installed translation.</p>@endforelse</div><div class="mt-6 flex justify-between"><a href="{{ route('bible.index', ['translation' => $translation?->abbreviation, 'book' => $book, 'chapter' => max(1, $chapter - 1)]) }}" class="grid size-9 place-items-center rounded-full border border-slate-200"><i data-lucide="chevron-left" class="size-4"></i></a><a href="{{ route('bible.index', ['translation' => $translation?->abbreviation, 'book' => $book, 'chapter' => $chapter + 1]) }}" class="grid size-9 place-items-center rounded-full border border-slate-200"><i data-lucide="chevron-right" class="size-4"></i></a></div></article><div class="border-t border-slate-100 bg-violet-50 p-5"><p class="text-xs font-black uppercase text-violet-700">Verse of the Day</p><p class="mt-1 font-semibold text-slate-800">{{ $dailyVerse?->text ?? 'No verse is available in this translation yet.' }}</p>@if($dailyVerse)<p class="mt-1 text-xs font-bold text-violet-700">{{ $dailyVerse->book }} {{ $dailyVerse->chapter }}:{{ $dailyVerse->verse }} ({{ $translation?->abbreviation }})</p>@endif</div></section>
            <aside class="space-y-3"><section class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><h2 class="text-sm font-black">Study Tools</h2>@foreach([['share-2','Cross References','bible.compare',''],['message-square','Commentaries','bible.search','commentaries'],['book-open','Dictionaries','bible.search','dictionaries'],['git-compare','Verse Comparison','bible.compare',''],['languages','Parallel Bible','bible.compare',''],['milestone','Bible Timeline','bible.search','bible-timeline'],['search','Topical Search','bible.search','']] as [$icon,$label,$route,$tool])@php($params = $route === 'bible.compare' ? ['book' => $book, 'chapter' => $chapter, 'verse' => $verses->first()?->verse ?? 1] : ['q' => $label === 'Topical Search' ? null : $book, 'tool' => $tool, 'book' => $book, 'chapter' => $chapter])<a href="{{ route($route, $params) }}" class="flex items-center gap-2 py-2.5 text-xs font-semibold"><i data-lucide="{{ $icon }}" class="size-4 text-violet-600"></i>{{ $label }}<i data-lucide="chevron-right" class="ml-auto size-3"></i></a>@endforeach</section><section class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div class="flex items-center justify-between"><h2 class="text-sm font-black">Quick Actions</h2><span x-show="selectedVerses.length" class="rounded-full bg-violet-100 px-2 py-1 text-[11px] font-black text-violet-700"><span x-text="selectedVerses.length"></span> selected</span></div><p x-show="!selectedVerses.length" class="mt-2 text-xs leading-5 text-slate-500">Select one or more verses to enable actions.</p><div x-show="selectedVerses.length" class="mt-3 grid gap-2"><button type="button" @click="openAction('bookmark')" class="flex items-center gap-2 rounded-lg bg-violet-50 px-3 py-2.5 text-left text-xs font-bold text-violet-700"><i data-lucide="bookmark-plus" class="size-4"></i>Bookmark</button><button type="button" @click="openAction('note')" class="flex items-center gap-2 rounded-lg bg-emerald-50 px-3 py-2.5 text-left text-xs font-bold text-emerald-700"><i data-lucide="notebook-pen" class="size-4"></i>Add Note</button><button type="button" @click="openAction('highlight')" class="flex items-center gap-2 rounded-lg bg-orange-50 px-3 py-2.5 text-left text-xs font-bold text-orange-700"><i data-lucide="highlighter" class="size-4"></i>Highlight</button><a x-bind:href="'{{ route('bible.compare') }}?book=' + encodeURIComponent(selectedVerses[0]?.book || '{{ $book }}') + '&chapter=' + (selectedVerses[0]?.chapter || {{ $chapter }}) + '&verse=' + (selectedVerses[0]?.verse || {{ $verses->first()?->verse ?? 1 }})" class="flex items-center gap-2 rounded-lg bg-blue-50 px-3 py-2.5 text-xs font-bold text-blue-700"><i data-lucide="share-2" class="size-4"></i>Cross References</a><button type="button" @click="selectedVerses = []; actionOpen = null" class="flex items-center gap-2 rounded-lg border border-slate-200 px-3 py-2.5 text-left text-xs font-bold text-slate-600"><i data-lucide="x" class="size-4"></i>Clear</button></div></section></aside>
            <aside class="space-y-3"><section class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div class="flex justify-between"><h2 class="text-sm font-black">My Notes</h2><a href="{{ route('bible.notes') }}" class="text-xs font-bold text-violet-700">View all</a></div>@if($recentNote)<div class="mt-3 rounded-lg border p-3"><strong class="text-xs">{{ $recentNote->reference }}</strong><p class="mt-2 text-xs text-slate-500">{{ Str::limit(strip_tags($recentNote->body), 130) }}</p></div>@else<p class="mt-3 text-sm text-slate-500">No notes yet.</p>@endif</section><section class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div class="flex justify-between"><h2 class="text-sm font-black">Highlights</h2><a href="{{ route('bible.highlights') }}" class="text-xs font-bold text-violet-700">View all</a></div><div class="mt-3 space-y-2">@forelse($recentHighlights as $highlight)<div class="rounded-lg border-l-4 border-violet-400 p-3"><strong class="text-xs">{{ $highlight->reference }}</strong><p class="mt-1 text-xs text-slate-500">{{ Str::limit($highlight->snippet, 100) }}</p></div>@empty<p class="text-sm text-slate-500">No highlights yet.</p>@endforelse</div></section><section class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><h2 class="text-sm font-black">Reading Plan Progress</h2>@if($activePlan)@php($pivot = $activePlan->users->first()->pivot)@php($planPercent = min(100, (int) round(($pivot->current_day / max(1, $activePlan->duration_days)) * 100)))<div class="mt-4 flex justify-between text-xs"><strong>{{ $activePlan->name }}</strong><span>{{ $planPercent }}%</span></div><div class="mt-2 h-2 rounded-full bg-slate-100"><div class="h-2 rounded-full bg-violet-600" style="width: {{ $planPercent }}%"></div></div><p class="mt-2 text-xs text-slate-500">Day {{ $pivot->current_day }} of {{ $activePlan->duration_days }} Â· {{ $pivot->current_streak }} day streak</p>@else<p class="mt-3 text-sm text-slate-500">No active reading plan.</p>@endif<a href="{{ route('bible.plans') }}" class="mt-3 block rounded-lg bg-violet-600 py-2.5 text-center text-xs font-bold text-white">{{ $activePlan ? 'Continue Reading' : 'Browse Reading Plans' }}</a></section></aside>
        </div>
        <div x-cloak x-show="actionOpen" @keydown.escape.window="actionOpen = null" class="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-4"><div @click.outside="actionOpen = null" class="w-full max-w-xl rounded-2xl bg-white p-6 shadow-2xl"><div class="flex items-start justify-between"><div><p class="text-xs font-bold uppercase text-violet-600" x-text="selectedReference"></p><h2 class="mt-1 text-xl font-black text-slate-950"><span x-show="actionOpen === 'bookmark'">Bookmark selected verses</span><span x-show="actionOpen === 'note'">Add note to selection</span><span x-show="actionOpen === 'highlight'">Highlight selected verses</span></h2></div><button type="button" @click="actionOpen = null" class="grid size-9 place-items-center rounded-lg border"><i data-lucide="x" class="size-4"></i></button></div><form x-show="actionOpen === 'bookmark'" method="POST" action="{{ route('bible.bookmarks.store') }}" class="mt-5 space-y-3">@csrf<input type="hidden" name="translation_id" value="{{ $translation?->id }}"><input type="hidden" name="reference" x-bind:value="selectedReference"><input type="hidden" name="book" x-bind:value="selectedVerses[0]?.book"><input type="hidden" name="chapter" x-bind:value="selectedVerses[0]?.chapter"><input type="hidden" name="verse" x-bind:value="selectedVerses[0]?.verse"><textarea name="preview" x-model="draftText" class="h-28 w-full rounded-lg border border-slate-200 p-3 text-sm"></textarea><button class="w-full rounded-lg bg-violet-600 py-3 text-sm font-bold text-white">Save Bookmark</button></form><form x-show="actionOpen === 'note'" method="POST" action="{{ route('bible.notes.store') }}" class="mt-5 space-y-3">@csrf<input type="hidden" name="translation_id" value="{{ $translation?->id }}"><input type="hidden" name="reference" x-bind:value="selectedReference"><input name="title" required placeholder="Note title" class="h-11 w-full rounded-lg border border-slate-200 px-3"><textarea name="body" required x-model="draftText" class="h-32 w-full rounded-lg border border-slate-200 p-3 text-sm"></textarea><button class="w-full rounded-lg bg-emerald-600 py-3 text-sm font-bold text-white">Save Note</button></form><form x-show="actionOpen === 'highlight'" method="POST" action="{{ route('bible.highlights.store') }}" class="mt-5 space-y-3">@csrf<input type="hidden" name="translation_id" value="{{ $translation?->id }}"><input type="hidden" name="reference" x-bind:value="selectedReference"><textarea name="snippet" required x-model="draftText" class="h-28 w-full rounded-lg border border-slate-200 p-3 text-sm"></textarea><div class="grid gap-3 sm:grid-cols-2"><select name="color" class="h-11 rounded-lg border border-slate-200 px-3"><option value="yellow">Yellow</option><option value="green">Green</option><option value="purple">Purple</option><option value="pink">Pink</option><option value="blue">Blue</option></select><input name="meaning" placeholder="Meaning or tag" class="h-11 rounded-lg border border-slate-200 px-3"></div><button class="w-full rounded-lg bg-orange-500 py-3 text-sm font-bold text-white">Save Highlight</button></form></div></div>
    </div>
</x-app-layout>
