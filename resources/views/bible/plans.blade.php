<x-app-layout title="Reading Plans" :breadcrumbs="$breadcrumbs" main-class="px-4 py-5 sm:px-6 lg:px-7"><div class="space-y-5"><div class="flex items-center justify-between"><div><h1 class="text-2xl font-black text-slate-950">Reading Plans</h1><p class="mt-1 text-sm text-slate-500">Stay consistent and grow deeper in God’s Word.</p></div><div class="flex gap-2"><button class="rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-bold text-slate-700">All Plans <i data-lucide="chevron-down" class="ml-1 inline size-4"></i></button><button class="rounded-lg bg-violet-600 px-4 py-2.5 text-sm font-bold text-white">Find a Plan</button></div></div><h2 class="text-base font-black text-slate-950">My Active Plans</h2><div class="grid gap-4 lg:grid-cols-3">@foreach($plans->take(3) as $plan)@php($membership = $plan->users->first())<article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between"><span class="grid size-11 place-items-center rounded-xl bg-violet-100 text-violet-600"><i data-lucide="book-open" class="size-5"></i></span><button class="text-slate-400"><i data-lucide="more-vertical" class="size-4"></i></button></div><h3 class="mt-4 font-black text-slate-950">{{ $plan->name }}</h3><p class="mt-1 min-h-10 text-sm text-slate-500">{{ $plan->description }}</p><div class="mt-5 h-2 rounded-full bg-slate-100"><div class="h-2 rounded-full bg-violet-600" style="width: {{ $membership ? min(100, round(($membership->pivot->current_day / $plan->duration_days) * 100)) : 0 }}%"></div></div><div class="mt-2 flex justify-between text-xs font-bold text-slate-500"><span>{{ $membership ? 'Day '.$membership->pivot->current_day : 'Not started' }} of {{ $plan->duration_days }}</span><span>{{ $membership ? round(($membership->pivot->current_day / $plan->duration_days) * 100) : 0 }}%</span></div><form method="POST" action="{{ route('bible.plans.start', $plan) }}" class="mt-5">@csrf<button class="w-full rounded-lg border border-violet-200 py-2.5 text-sm font-bold text-violet-700 hover:bg-violet-50">{{ $membership ? 'Continue Plan' : 'Start Plan' }} <i data-lucide="arrow-right" class="ml-1 inline size-4"></i></button></form></article>@endforeach</div><h2 class="pt-3 text-base font-black text-slate-950">Recommended Plans</h2><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach($plans as $plan)<div class="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><span class="grid size-9 place-items-center rounded-lg bg-sky-50 text-sky-600"><i data-lucide="calendar-days" class="size-4"></i></span><h3 class="mt-3 font-black text-slate-900">{{ $plan->name }}</h3><p class="mt-1 text-xs text-slate-500">{{ $plan->duration_days }} days · {{ $plan->category }}</p></div>@endforeach</div></div></x-app-layout>
