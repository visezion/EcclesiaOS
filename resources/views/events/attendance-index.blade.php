<x-app-layout title="Attendance" :breadcrumbs="$breadcrumbs">
    <div class="space-y-5"><div class="flex items-center gap-4"><div class="grid size-14 place-items-center rounded-lg bg-violet-100 text-violet-600"><i data-lucide="clipboard-check" class="size-7"></i></div><div><h1 class="text-2xl font-semibold text-slate-950">Attendance Sessions</h1><p class="text-sm text-slate-500">Manage attendance windows, methods, verification evidence, and final records.</p></div></div><section class="rounded-lg border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Session</th><th class="px-5 py-3">Program</th><th class="px-5 py-3">Methods</th><th class="px-5 py-3">Records</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($attendanceSessions as $session)<tr><td class="px-5 py-4">{{ $session->title }}<div class="text-xs text-slate-500">{{ $session->eventSession?->session_date?->format('M d, Y') }}</div></td><td class="px-5 py-4">{{ $session->eventSession?->event?->program?->name }}</td><td class="px-5 py-4">{{ collect($session->methods)->map(fn($m) => Str::headline($m))->join(', ') }}</td><td class="px-5 py-4">{{ $session->records->count() }}</td><td class="px-5 py-4"><span class="rounded-full bg-violet-50 px-2.5 py-1 text-xs text-violet-700">{{ Str::headline($session->status) }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('event-sessions.attendance', $session->eventSession) }}" class="text-violet-600">Manage</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No attendance sessions yet.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 p-4">{{ $attendanceSessions->links() }}</div></section></div>
</x-app-layout>
