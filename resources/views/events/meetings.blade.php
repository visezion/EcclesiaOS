<x-app-layout title="Meetings" :breadcrumbs="$breadcrumbs">
    <div class="space-y-5">
        <div class="flex items-center justify-between"><div class="flex items-center gap-4"><div class="grid size-14 place-items-center rounded-lg bg-violet-100 text-violet-600"><i data-lucide="video" class="size-7"></i></div><div><h1 class="text-2xl font-semibold text-slate-950">Meetings</h1><p class="text-sm text-slate-500">Physical and built-in online meetings connected to event sessions and attendance.</p></div></div><a href="{{ route('meeting-integrations.index') }}" class="rounded-lg border border-slate-200 bg-white px-4 py-2.5 text-sm text-slate-700">Built-in Setup</a></div>
        <section class="rounded-lg border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Meeting</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Date</th><th class="px-5 py-3">Venue / Online</th><th class="px-5 py-3">Attendance</th><th class="px-5 py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($sessions as $session)<tr><td class="px-5 py-4"><a class="text-violet-700" href="{{ route('event-sessions.meeting', $session) }}">{{ $session->title }}</a><div class="text-xs text-slate-500">{{ $session->event->program?->name }}</div></td><td class="px-5 py-4">{{ Str::headline($session->meeting_type) }}</td><td class="px-5 py-4">{{ $session->session_date->format('M d, Y') }} {{ Str::of($session->starts_at)->substr(0,5) }}</td><td class="px-5 py-4">{{ $session->venue ?: 'Online room' }}</td><td class="px-5 py-4">{{ $session->attendanceSession?->records_count ?? $session->attendanceSession?->records()->count() ?? 0 }}</td><td class="px-5 py-4 text-right"><a href="{{ route('event-sessions.meeting', $session) }}" class="inline-grid size-8 place-items-center rounded-lg hover:bg-violet-50"><i data-lucide="settings" class="size-4"></i></a><a href="{{ route('event-sessions.attendance', $session) }}" class="inline-grid size-8 place-items-center rounded-lg hover:bg-violet-50"><i data-lucide="clipboard-check" class="size-4"></i></a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No meetings scheduled.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 p-4">{{ $sessions->links() }}</div></section>
    </div>
</x-app-layout>
