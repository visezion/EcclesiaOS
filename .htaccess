<IfModule mod_rewrite.c>
    RewriteEngine On

    # Only Laravel's public directory may be served by the web server.
    RewriteRule ^$ public/ [R=302,L]
    RewriteRule !^public(?:/|$) - [F,L,NC]
</IfModule>

<IfModule !mod_rewrite.c>
    Require all denied
</IfModule>
